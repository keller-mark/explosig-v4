import React, { useRef } from "react";
import Plot from "./Plot.js";
import Two from "../../two.js";


function drawBarPlot(domElement) {
    const two = new Two({
        width,
        height,
        domElement
    });
}

export default function BarPlot(props) {
    const { drawRegister } = props;

    
    return (
        <Plot
            draw={}
        />
    );
}